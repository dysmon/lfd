apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: {{ prometheus_url | default("http://localhost:9090") }}
    isDefault: true

  - name: Loki
    type: loki
    access: proxy
    url: {{ loki_url | default("http://localhost:3100") }}